spring:
  profiles:
    default: dev
---
spring:
  config:
    activate:
      on-profile: dev
    import: "optional:configserver:http://configUser:configPassword123@localhost:8888"
---
spring:
  config:
    activate:
      on-profile: prod
    import: "optional:configserver:http://configUserProd:configPasswordProd123@configserver:8888"
