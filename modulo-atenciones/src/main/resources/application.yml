spring:
  application:
    name: modulo-atenciones
  cloud:
      config:
        profile: dev
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
        defaultZone: http://eurekaserver:8761/eureka/