spring:
  profiles:
    default: dev
---
spring:
  config:
    activate:
      on-profile: dev
    import: "optional:configserver:http://configUser:configPassword123@localhost:8888"
---
spring:
  config:
    activate:
      on-profile: prod
    import: "optional:configserver:http://configserver:8888"